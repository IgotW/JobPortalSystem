@model IEnumerable<JobSystem.Entities.JobPosting>

@{
    ViewData["Title"] = "CandidateDashPage";
}

<h1>CandidateDashPage</h1>

@* <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostedDate)
            </td>
            <td>
                <a asp-action="Apply" asp-route-id="@item.Id">Apply</a> |
                <a asp-action="DetailsCan" asp-route-id="@item.Id">Details</a>
            </td>
        </tr>
}
    </tbody>
</table> *@
<div class="container-fluid py-4">
    <div class="row">
        <!-- Left Sidebar - Filters -->
        <div class="col-md-3">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <h5 class="mb-3">Filters</h5>
                    <form>
                        <div class="mb-3">
                            <label class="form-label">Experience Level</label>
                            <select class="form-select">
                                <option>Entry Level</option>
                                <option>Mid Level</option>
                                <option>Senior Level</option>
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Job Type</label>
                            <div>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" id="fullTime">
                                    <label class="form-check-label" for="fullTime">Full-time</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" id="partTime">
                                    <label class="form-check-label" for="partTime">Part-time</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" id="remote">
                                    <label class="form-check-label" for="remote">Remote</label>
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Salary Range</label>
                            <select class="form-select">
                                <option>$0 - $50k</option>
                                <option>$50k - $100k</option>
                                <option>$100k - $150k</option>
                                <option>$150k+</option>
                            </select>
                        </div>

                        <button type="submit" class="btn btn-primary w-100">Apply Filters</button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Main Content - Job Listings -->
        <div class="col-md-9">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <form class="d-flex gap-2">
                        <input type="search" class="form-control" placeholder="Search jobs...">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-search"></i> Search
                        </button>
                    </form>
                </div>
            </div>
       </div>
    </div>
</div>
<div class="row g-4">
    @foreach (var job in Model)
    {
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h5 class="card-title">@job.Title</h5>
                            <h6 class="card-subtitle mb-2 text-muted">
                                @job.CompanyName • @job.Location
                            </h6>
                        </div>
                        <div class="btn-group">
                            <a asp-action="Apply" asp-route-id="@job.Id" class="btn btn-sm btn-outline-primary">
                                <i class="bi bi-pencil"></i> Apply
                            </a>
                            <a asp-action="DetailsCan" asp-route-id="@job.Id" class="btn btn-sm btn-outline-info">
                                <i class="bi bi-eye"></i> Details
                            </a>
                        </div>
                    </div>

                    <div class="mb-2">
                        <span class="badge bg-success me-2">@job.Type</span>
                        <span class="badge bg-info">@job.Salary</span>
                    </div>

                    <p class="card-text">@job.Description</p>
                    <small class="text-muted">Posted @job.PostedDate.ToString("MMM dd, yyyy")</small>
                </div>
            </div>
        </div>
    }
</div>
